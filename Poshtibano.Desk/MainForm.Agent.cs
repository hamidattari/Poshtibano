namespace Poshtibano.Desk
{
    partial class MainForm
    {
        #region Agent-Specific Setup

        private void SetupAgentUI()
        {
            // Agent doesn't need special UI setup beyond showing dashboard
            labelInfo.Visible = true;
        }

        #endregion
    }
}